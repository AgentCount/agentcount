[package]
name = "sweeper"
version = "0.2.0"
edition = "2024"

[dependencies]
chain = { path = "../chain" }
checks = { path = "../checks" }
payments = { path = "../payments" }
probe = { path = "../probe" }
alloy = { workspace = true }
sqlx = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
futures = { workspace = true }

# One HTTP GET, in `bin/heartbeat.rs`: the dead man's switch pings an external
# monitor. Deliberately not routed through `crates/probe` — that client carries
# robots.txt handling, an SSRF netguard and a per-host cap, all correct for
# fetching a stranger's document and all meaningless for calling a URL we
# configured ourselves.
reqwest = { workspace = true }
