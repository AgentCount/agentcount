# Rust build output — big, regenerable, never committed.
/target

# Local environment: secrets, RPC keys, DATABASE_URL. Never commit this.
.env

/docs
.env.local

# Run exports: reproducible from Postgres + the pinned commit, never committed.
/data
